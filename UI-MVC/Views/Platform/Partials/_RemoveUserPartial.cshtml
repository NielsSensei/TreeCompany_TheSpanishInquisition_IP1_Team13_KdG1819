@model AssignUserModel

 @using (Html.BeginForm("RemoveUserFromPlatform", "Platform", FormMethod.Post))
{
    @Html.HiddenFor(m => m.PlatformId, "PlatformId")
    @Html.HiddenFor(m => m.UserMail, "UserMail")
    <input type="submit" class="btn btn-danger h-25" value="-"/>
}