@using Domain.UserInput
@using Domain.Projects

@model Domain.Projects.Project
@{ 
    IEnumerable<Domain.Projects.Project> projects = (IEnumerable<Domain.Projects.Project>)ViewData["projects"];
    Questionnaire newQ = (Questionnaire)ViewData["questionnaire"];
}

@{
    Layout = "_Layout";
}

<h1>Een nieuwe vragenlijst maken voor project @Model.Title</h1>
  
        <div class="form-group">
            
            <label for="sel2">Selecteer een fase:</label>
            <select class="form-control" id="sel2">
               @foreach (Phase phase in ViewBag.Project)
               {
                   <option>@phase.Description</option>
               }
            </select>

            <form asp-controller="Admin" asp-action="CreateQuestionnaire" method="post">
                <label>Title</label>
                <input type="text" name="name" value="@ViewBag.Questionnaire.Title" />
            </form>

        </div>

    






</div>





